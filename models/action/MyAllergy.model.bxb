action (MyAllergy) {
  description (대두 알레르기 등록/삭제 해줘)
  collect {

    input (myMaterial) {
      type (MyMaterial)
      min (Optional) max (One)
    }

    input (myCommand) {
      type (MyCommand)
      min (Optional) max (One)
    }
  }

  type(Search)
  output (MyAllergyResults)
}
