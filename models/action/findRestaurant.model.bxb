action (findRestaurant) {
  description (__DESCRIPTION__)
  collect {
    input (restaurantLocation) {
      type (RestaurantLocation)
      min (Optional) max (One)
    }
    input (restaurantName) {
      type (RestaurantName)
      min (Optional) max (One)
    }
  }
  type(Search)
  output (findRestaurantResualts)
}
