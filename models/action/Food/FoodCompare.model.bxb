action (FoodCompare) {
	description (진라면이랑 비교해줘)
	type(Calculation)
	collect {
		input (food) {
			type (FoodOne)
			min (Required) max (One)
			// default-init {
			// 	intent {
			// 		goal: FoodSearch
			// 		value: FoodName(신라면)
			// 	}
			// }
		}
		input(foodTwo) {
			type (FoodTwo)
			min(Required) max(One)
			default-init {
				intent {
					goal {
						FoodSearch
					}
				}
			}
		}
	}
	output (CompareResult)
}

