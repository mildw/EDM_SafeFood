action (DeleteMyFood) {
  description ("Delete user data from remote db")
  type (Calculation)
  collect {
    input (food) {
      type (Food)
      min (Required) max (One)
      default-init {
        intent {
         goal: GetMyFood
        }
      }
    }
  }
  output (core.Boolean)
}