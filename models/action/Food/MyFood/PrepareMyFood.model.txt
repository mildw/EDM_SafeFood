action (PrepareMyFood) {
  description ("Prepare user data based on your business logic")
  type (Constructor)
  collect {
    // Keep this in place to retrieve any previously stored UserData
    input (previousMyFood) {
      type (MyFood)
      min (Optional) max (One)
      default-init {
        intent {
          goal: GetMyFood
        }
      }
    }
    // Your domain specific inputs
    input (food) {
      description ("Example of a single input to replace a MyFood property.")
      type (Food)
      min (Optional) max (One)
    }
  }
  output (MyFood)
}
