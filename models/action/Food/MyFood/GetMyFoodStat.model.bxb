action (GetMyFoodStat) {
	description ("사용자 섭취 식품 통계")
	type (Calculation)
	collect {
		input (food) {
			type (FoodOne)
			min (Required) max (Many)
			//input 이 없을 때, 현재 값을 보여줌(?)
			// default-init {
			// 	intent {
			// 		goal: GetMyFoodList
			// 	}
			// }
		}
	}
	output (MyFoodImg)
}
