action (FindHandle) {
  description (미미한 알레르기 반응 대처법 알려줘)
  type(Search)
  collect {
    input (findHandleWayL) {
      type (FindHandleWayL)
      min (Optional) max (One)
    }

    input (findHandleWayH) {
      type (FindHandleWayH)
      min (Optional) max (One)
    }
  }

  output (FindHandleResults)
}